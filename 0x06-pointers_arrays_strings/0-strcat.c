#include "main.h"

/**
 * _strcat - function
 * @dest : first string
 * @src : second string
 * Return: @dest + @src
 */

char *_strcat(char *dest, char *src)
{
	char *con;

	_putchar("%s%s", dest, src);
	con = &dest

	return (con);
}
